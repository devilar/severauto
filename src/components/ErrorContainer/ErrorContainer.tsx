import React from 'react';
import { Alert } from '@mui/material';

const ErrorContainer = () => {
  return (
    <Alert style={{marginTop:'10px'}} severity="error">Test!!!</Alert>
  );
};

export default ErrorContainer;